# 09 -- Commit hook (meshcore-planner)

A `git commit` in meshcore-planner runs the code-check hook. If it reports
findings, fix each one or justify it in your PR body as an intentional
exception. Do not `--no-verify` past it without explicit authorization.
